name: "Retired: Jules Burst Monitor"

# The burst loop amplified one dispatch into repeated monitor/recovery events.
# This compatibility workflow is manual, read-only and intentionally inert.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Explain authoritative replacement
        run: |
          echo "Jules Burst Monitor is retired."
          echo "The durable reconciler observes progress on its bounded five-minute schedule."

