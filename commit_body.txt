This PR resolves the task `proxy-tools-strip-claude-code-newlines-edge-case-test` by adding an explicit edge-case test case for testing how Claude Code strips excess newlines placed around strings. We also add a small `proxy-tools-is-suggestion-mode-tests` task to manifest queue to meet the task replenishment limit.

<!-- AUTONOMOUS_TASK_EVIDENCE
task_id: proxy-tools-strip-claude-code-newlines-edge-case-test
status: done
acceptance:
- Verify an edge-case test for newline cleanup is added at string boundaries -> added test case 'cleans up extra newlines at string boundaries' in internal/proxy/tools_test.go
evidence_files:
- internal/proxy/tools_test.go
- agent_tasks.json
checks:
- go test ./internal/proxy -run TestStripClaudeCodeInstructions_PreservesCodingIntent
- python3 scripts/validate_agent_tasks.py agent_tasks.json
micro_pr_justification: The selected task targets a specific testing boundary issue and provides isolated validation. Grouping this test prevents regressions when Claude Code integration tags change logic.
-->
