Fix automation health finding: repeated_followup_generation (blocked)

<!-- AUTONOMOUS_TASK_EVIDENCE
task_id: automation-health-repeated-followup-generation-6dc5c3e5
status: blocked
blocked_reason: The root cause for the `repeated_followup_generation` finding false positive is in `.github/scripts/automation-health-report.py`, which is missing the `FOLLOWUP_IDENTIFIER_RE` mask and `automation-health-` exemption to ignore task IDs, but `.github/scripts/automation-health-report.py` is outside the allowed_paths.
acceptance:
- Root cause for Automation Health finding `repeated_followup_generation` is identified using automation-health artifacts, CI checks, Jules session data, or manifest evidence. -> Blocked due to out-of-scope root cause.
- The implementation fixes the automation issue or blocks this task with a concrete blocked_reason naming missing evidence, permissions, or external API instability. -> Task blocked with concrete blocked_reason in agent_tasks.json
- A follow-up Automation Health run no longer reports `repeated_followup_generation`, or the PR body explains why the finding is intentionally tolerated. -> Created follow-up task targeting automation-health-report.py
evidence_files:
- agent_tasks.json
checks:
- python3 scripts/validate_agent_tasks.py agent_tasks.json
micro_pr_justification: This PR correctly marks a task blocked that cannot be completed within scope and schedules the proper fix in a follow-up task.
-->
