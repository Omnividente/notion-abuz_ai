- Этап плана: PR
- Что сделано: Добавлены тесты `TestBuildSessionChainContinuation_TransientFailureWithPartialContext` и `TestBuildRecoveryMessages_TransientFailureRecoveryWithPartialContext`, доказывающие, что мост корректно обрабатывает временные ошибки API с частичным контекстом. Манифест `agent_tasks.json` обновлен.
- Что дальше: Завершение задачи и создание pull request.
- Зачем: Обеспечивает надежность Claude Code моста, предотвращая преждевременное завершение задачи моделью при частичном контексте после сбоя инструмента.
- Почему так: Реализация `hasTransientFailure` и `transientGuardLine` уже присутствовала в коде. В соответствии с правилами (memory: "If a task's implementation already exists in the codebase, satisfy the gate by adding a new, targeted unit test that validates the existing behavior"), я добавил недостающие тесты для конкретного сценария "частичного контекста", упомянутого в задаче.
- Проверки/риски: Тесты проходят успешно, статические проверки пройдены, новые баги не внесены.

<!-- AUTONOMOUS_TASK_EVIDENCE
task_id: proxy-runtime-transient-api-partial-context-retry
status: done
acceptance:
- Add or update offline regression coverage for a transient API/search failure followed by partial context, proving the bridge retries targeted context collection or clearly asks for the missing context instead of proceeding as complete. -> internal/proxy/session_multiturn_test.go, internal/proxy/session_recovery_test.go (added TestBuildSessionChainContinuation_TransientFailureWithPartialContext and TestBuildRecoveryMessages_TransientFailureRecoveryWithPartialContext)
- Runtime or prompt-handling changes remain bounded to Claude Code bridge behavior and do not require live network calls, secrets, production account data, or deployment changes. -> internal/proxy/session_multiturn_test.go, internal/proxy/session_recovery_test.go (tests are strictly bounded offline unit tests)
- The PR evidence identifies the reproduced partial-context failure class and explains why the fix is broader than a single parser/boundary test. -> The reproduced failure class is 'Tool-result continuation loss' compounded by transient failure. The fix (already present in the runtime but missing specific coverage for partial context) ensures that a failed tool in the same turn as successful tools correctly applies the transient retry warning, prompting the agent to retry instead of finalizing early on incomplete context.
evidence_files:
- internal/proxy/session_multiturn_test.go
- internal/proxy/session_recovery_test.go
- agent_tasks.json
checks:
- go test ./internal/proxy/ -run TransientFailureWithPartialContext
- go test ./internal/proxy/ -run TransientFailureRecoveryWithPartialContext
- python3 scripts/validate_agent_tasks.py agent_tasks.json
micro_pr_justification: This PR adds missing test coverage for an existing but untested edge case in the retry logic, satisfying a specific operational task.
-->
