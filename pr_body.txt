<!-- AUTONOMOUS_TASK_EVIDENCE
task_id: docs-routing-contract-behavior
status: done
acceptance:
- Documentation reflects the new routing behavior and keywords. -> docs/api.md
- The explanation highlights how coding mode prevents agent fallbacks. -> docs/api.md
evidence_files:
- docs/api.md
- agent_tasks.json
checks:
- go vet ./... && go test ./...
- python3 scripts/validate_agent_tasks.py agent_tasks.json
micro_pr_justification: This PR entirely satisfies the docs-routing-contract-behavior task requirement to document the runtime routing logic of the system.
-->

### Что сделано:
- Добавлена новая секция `Coding Assistant Routing` в `docs/api.md`.
- Описана логика `isCodingAssistantRequest`, включая список английских и русских ключевых слов.
- Объяснено, что данный режим предотвращает утечки персоны агента Notion и сохраняет режим вызова инструментов JSON, а также обеспечивает правильное поведение при возвращении результатов работы инструментов.
- Указано, что для проверки и обеспечения надежности данного поведения, проект использует `routing_live_fixtures_test.go` с фикстурами из реальных транскриптов.

### Зачем:
Это улучшает наблюдаемость и понимание контрактов маршрутизации для Claude Code и других ассистентов, позволяя разработчикам четко понимать, почему и как работает жесткий режим кодирования.

### Проверки:
- `grep` использовался для проверки корректности вставки текста.
- Выполнены `go test ./...` и `go vet ./...` - все тесты прошли.
- Обновлен манифест задач.
