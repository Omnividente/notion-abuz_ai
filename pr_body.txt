<!-- AUTONOMOUS_TASK_EVIDENCE
task_id: proxy-tools-add-unit-tests-for-debug-logging-toggle
status: done
acceptance:
- A unit test exists that verifies tool schema simplification logs are muted when DebugLoggingEnabled() is false. -> internal/proxy/tools_test.go (TestDebugLoggingMuted_ToolSchemaSimplification)
- A unit test exists that verifies JSON tool-call mode fallback logs are muted when DebugLoggingEnabled() is false. -> internal/proxy/tools_test.go (TestDebugLoggingMuted_JSONToolModeFallback)
evidence_files:
- internal/proxy/tools_test.go
- agent_tasks.json
checks:
- go test -v ./internal/proxy -run TestDebugLoggingMuted
micro_pr_justification: this PR fully completes the task id `proxy-tools-add-unit-tests-for-debug-logging-toggle` by supplying the required test cases in a single operation
-->

Добавлены unit-тесты в `internal/proxy/tools_test.go` для проверки того, что логи отладки в `tools.go` скрываются при выключенном `DebugLoggingEnabled()`.
- Добавлен тест `TestDebugLoggingMuted_ToolSchemaSimplification`, который проверяет отключение логов при обрезке длинных описаний схем.
- Добавлен тест `TestDebugLoggingMuted_JSONToolModeFallback`, который проверяет скрытие логов при фолбэке из режима вызова инструментов JSON.
Это предотвращает лишнее замусоривание вывода логов, когда отладка выключена, при этом сохраняя нужную информацию при ее включении.
